/**
 * @param {string} s
 * @return {number}
 */
var romanToInt = function(s) {
    const hm={I:1, V:5, X:10, L :50, C :100, D :500, M :1000}
    let n=s.length
    for (let i=0;i<n;i++){
        
    }
};


module.exports={romanToInt}